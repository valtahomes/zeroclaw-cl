# Markdown 测试内容

## 1. 代码块测试

### Rust 代码
```rust
fn quicksort(arr: &mut [i32]) {
    let len = arr.len();
    if len <= 1 {
        return;
    }
    let pivot_index = partition(arr);
    let (left, right) = arr.split_at_mut(pivot_index);
    quicksort(left);
    quicksort(&mut right[1..]);
}

fn partition(arr: &mut [i32]) -> usize {
    let pivot = arr[arr.len() - 1];
    let mut i = 0;
    for j in 0..arr.len() - 1 {
        if arr[j] <= pivot {
            arr.swap(i, j);
            i += 1;
        }
    }
    arr.swap(i, arr.len() - 1);
    i
}
```

### Python 代码
```python
def hello_world():
    """一个简单的函数"""
    items = [1, 2, 3, 4, 5]
    for item in items:
        print(f"Item: {item}")
    return sum(items)

result = hello_world()
print(f"Sum: {result}")
```

### JavaScript 代码
```javascript
async function fetchData(url) {
    try {
        const response = await fetch(url);
        const data = await response.json();
        console.log('Data:', data);
        return data;
    } catch (error) {
        console.error('Error:', error);
    }
}
```

## 2. 行内代码

使用 `console.log()` 输出调试信息，或者用 `npm install` 安装包。

Rust 中的 `Result<T, E>` 类型用于错误处理。

## 3. 列表测试

### 无序列表
- 第一项
- 第二项
  - 子项 A
  - 子项 B
    - 嵌套子项 1
    - 嵌套子项 2
- 第三项

### 有序列表
1. 第一步：初始化项目
2. 第二步：安装依赖
3. 第三步：编写代码
   1. 3.1 创建模块
   2. 3.2 实现功能
4. 第四步：测试运行

### 任务列表
- [x] 已完成的任务
- [ ] 待办事项 1
- [ ] 待办事项 2
- [x] 已完成的任务 2

## 4. 表格测试

| 语言 | 类型 | 年份 | 特点 |
|------|------|------|------|
| Rust | 系统编程 | 2010 | 内存安全 |
| Python | 脚本语言 | 1991 | 易学易用 |
| JavaScript | Web 语言 | 1995 | 浏览器原生 |
| Go | 系统编程 | 2009 | 并发友好 |

## 5. 引用块

> 这是一段引用文本。
> 
> 可以包含多段内容。
> 
> > 甚至可以嵌套引用。

## 6. 强调文本

这是 **粗体** 文本，这是 *斜体* 文本，这是 ***粗斜体*** 文本。

这是 ~~删除线~~ 文本。

## 7. 链接和图片

[ZeroClaw GitHub](https://github.com/zero-claw/ZeroClaw)

[Rust 官网](https://www.rust-lang.org "Rust Programming Language")

## 8. 分隔线

---

上面和下面都是分隔线

---

## 9. 混合内容示例

### 算法复杂度对比

| 算法 | 最好情况 | 平均情况 | 最坏情况 |
|------|----------|----------|----------|
| 快速排序 | O(n log n) | O(n log n) | O(n²) |
| 归并排序 | O(n log n) | O(n log n) | O(n log n) |
| 堆排序 | O(n log n) | O(n log n) | O(n log n) |

### 使用示例

```python
# 快速排序使用示例
arr = [64, 34, 25, 12, 22, 11, 90]
quicksort(arr)
print(arr)  # 输出：[11, 12, 22, 25, 34, 64, 90]
```

> **提示**：快速排序在实际应用中通常是最快的，因为它的常数因子较小。

---

## 10. 特殊字符转义

测试特殊字符：& < > " '

测试美元符号：$100, $200

测试反斜杠：C:\Windows\System32

---

测试结束！
